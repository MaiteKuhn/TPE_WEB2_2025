<?php
require 'templates/header.phtml';
?>
<link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
<style>
    .prop-cards-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
        font-family: 'Montserrat', Arial, sans-serif;
    }
    .prop-cards-row {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 24px;
        width: 100%;
    }
    .prop-card {
        background: #fff;
        border: 1.5px solid #0033a0;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.06);
        min-width: 270px;
        max-width: 320px;
        padding: 24px 20px 18px 20px;
        margin-bottom: 18px;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
    }
    .prop-card h5 {
        color: #d32f2f;
        font-weight: 700;
        margin-bottom: 10px;
    }
    .prop-card strong {
        color: #0033a0;
    }
    .prop-card ul {
        padding-left: 18px;
        margin: 5px 0 0 0;
    }
    .prop-card ul li {
        margin-bottom: 5px;
    }
    .btn-volver {
        font-weight: 600;
        border-radius: 5px;
        margin-top: 8px;
        font-size: 0.98em;
        text-decoration: none;
        padding: 7px 16px;
        border: 2px solid #0033a0;
        color: #0033a0;
        transition: background 0.2s, color 0.2s;
    }
    .btn-volver:hover {
        background: #0033a0;
        color: #fff;
    }
</style>

<div class="prop-cards-container">
    <div class="prop-cards-row">
        <div class="prop-card">
            <h5><?= htmlspecialchars($propietario->nombre) ?></h5>
            <div style="margin-bottom: 10px;">
                <strong>Email:</strong> <?= htmlspecialchars($propietario->mail ?? '') ?><br>
                <strong>Teléfono:</strong> <?= htmlspecialchars($propietario->telefono ?? '') ?><br>
            </div>

            <h6>Propiedades de <?= htmlspecialchars($propietario->nombre) ?>:</h6>
            <?php if (!empty($propiedades)): ?>
                <ul>
                    <?php foreach ($propiedades as $propiedad): ?>
                        <li>
                            <?= htmlspecialchars($propiedad->tipo_propiedad) ?> - 
                            <?= htmlspecialchars($propiedad->ubicacion) ?> - 
                            Habitaciones: <?= htmlspecialchars($propiedad->habitaciones) ?> - 
                            Metros: <?= htmlspecialchars($propiedad->metros_cuadrados) ?>
                        </li>
                    <?php endforeach; ?>
                </ul>
            <?php else: ?>
                <p>Este propietario no tiene propiedades asociadas.</p>
            <?php endif; ?>

            <a class="btn-volver" href="<?= BASE_URL ?>propietarios/">Volver</a>
        </div>
    </div>
</div>

<?php require 'templates/footer.phtml'; ?>
