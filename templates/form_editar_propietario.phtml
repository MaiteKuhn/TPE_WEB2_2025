<?php require 'templates/header.phtml' ?>

<link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
<style>
    .editar-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 60vh;
    }
    .editar-form {
        background: #fff;
        border: 1.5px solid #0033a0;
        border-radius: 8px;
        padding: 32px 28px 24px 28px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.06);
        min-width: 320px;
        max-width: 400px;
        font-family: 'Montserrat', Arial, sans-serif;
    }
    .editar-form label {
        font-weight: 600;
        color: #0033a0;
    }
    .editar-form input[type="text"],
    .editar-form input[type="email"],
    .editar-form input[type="number"] {
        border: 1.5px solid #0033a0;
        border-radius: 5px;
        padding: 8px 10px;
        margin-bottom: 16px;
        width: 100%;
        font-size: 1rem;
    }
    .editar-form button[type="submit"] {
        background: #0033a0;
        color: #fff;
        border: 2px solid #0033a0;
        border-radius: 5px;
        padding: 8px 18px;
        font-weight: 600;
        font-size: 1rem;
        transition: background 0.2s, color 0.2s;
        margin-right: 10px;
    }
    .editar-form button[type="submit"]:hover {
        background: #fff;
        color: #0033a0;
    }
    .editar-form .btn-cancelar {
        background: #fff;
        color: #0033a0;
        border: 2px solid #0033a0;
        border-radius: 5px;
        padding: 8px 18px;
        font-weight: 600;
        font-size: 1rem;
        text-decoration: none;
        transition: background 0.2s, color 0.2s;
    }
    .editar-form .btn-cancelar:hover {
        background: #0033a0;
        color: #fff;
    }
</style>

<div class="editar-container">
    <form action="<?= BASE_URL ?>editarPropietario/<?= $propietario->id_propietario ?>" method="POST" class="editar-form">
        <div class="form-group">
            <label for="nombre">Nombre</label>
            <input type="text" id="nombre" name="nombre" value="<?= htmlspecialchars($propietario->nombre) ?>" required>
        </div>
        <div class="form-group">
            <label for="mail">Email</label>
            <input type="email" id="mail" name="mail" value="<?= htmlspecialchars($propietario->mail) ?>" required>
        </div>
        <div class="form-group">
            <label for="telefono">Teléfono</label>
            <input type="text" id="telefono" name="telefono" value="<?= htmlspecialchars($propietario->telefono) ?>" required>
        </div>
        <button type="submit">Editar</button>
        <a href="<?= BASE_URL ?>propietarios" class="btn-cancelar">Cancelar</a>
    </form>
</div>

<?php require 'templates/footer.phtml' ?>
