<?php require 'templates/header.phtml' ?>

<div class="editar-container">
    <form action="<?= BASE_URL ?>editarPropiedad/<?= $propiedad->id_propiedad ?>" method="POST" class="editar-form">
        <div class="form-group">
            <label for="id_propietario_fk">Propietario</label>
            <select id="id_propietario_fk" name="id_propietario_fk" required>
                <?php foreach ($propietarios as $propietario): ?>
                    <option value="<?= $propietario->id_propietario ?>" <?= isset($propiedad) && $propietario->id_propietario == $propiedad->id_propietario_fk ? 'selected' : '' ?>>
                        <?= htmlspecialchars($propietario->nombre) ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>
        <div class="form-group">
            <label for="tipo_propiedad">Tipo de propiedad</label>
            <input type="text" id="tipo_propiedad" name="tipo_propiedad" value="<?= htmlspecialchars($propiedad->tipo_propiedad) ?>" required>
        </div>
        <div class="form-group">
            <label for="ubicacion">Ubicación</label>
            <input type="text" id="ubicacion" name="ubicacion" value="<?= htmlspecialchars($propiedad->ubicacion) ?>" required>
        </div>
        <div class="form-group">
            <label for="habitaciones">Cantidad de habitaciones</label>
            <input type="number" id="habitaciones" name="habitaciones" value="<?= $propiedad->habitaciones ?>" required>
        </div>
        <div class="form-group">
            <label for="metros_cuadrados">Metros cuadrados</label>
            <input type="number" id="metros_cuadrados" name="metros_cuadrados" value="<?= $propiedad->metros_cuadrados ?>" required>
        </div>
        <button type="submit">Editar</button>
        <a href="<?= BASE_URL ?>propiedades" class="btn-cancelar">Cancelar</a>
    </form>
</div>

<?php require 'templates/footer.phtml' ?>